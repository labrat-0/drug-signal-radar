[project]
name = "drug-signal-radar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "apify>=2.0.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
